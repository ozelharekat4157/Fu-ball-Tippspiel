{% extends 'base.html.twig' %}

{# {% block title %}Hello MainCo{% endblock %} #}

{% block body %}


    {# <h1>Hello {{ controller_name }}! ✅</h1>

    <p>SERHAT</p>

    <button> serhat </button> #}


    <div class="container">
       {% for message in app.flashes('notice') %}
        <div class="alert alert-success mt-4 mb-b">
              {{message}}
          </div>
  
    {%endfor%}

        <table class="table">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">Verein</th>      
      <th scope="col">Stadt</th>
      <th scope="col">Action</th>
    </tr>
  </thead>
  <tbody>
  {% for data in list %}
    <tr>
      <th scope="row">{{ data.id }}</th>
      <td>{{ data.title }}</td>       
      <td>{{ data.content }}</td>
      <td>
      <a class="btn btn-warning" href="{{path('update',{'id': data.id }) }}"> Update </a>
      <a class="btn btn-danger"  href="{{path('delete',{'id': data.id }) }}"> Delete </a>
      </td>
    </tr>
{% endfor %}
  </tbody>
</table>
    
    </div>

 {% endblock %}


{#  *********  ADRES   *********   http://localhost/crud/public/index.php/main #}
